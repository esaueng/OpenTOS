# Solver execution mode:
# - browser: in-tab Web Worker solver (no backend required)
# - api: study/job endpoints (/api/studies, /api/jobs) on the backend
VITE_SOLVER_MODE=browser

# Only used when VITE_SOLVER_MODE=api
# Example: https://your-api-domain.example.com
VITE_API_BASE=
