# browser: local Web Worker preview, no API required
# api: durable project/model/study/run flow through /api/v3
VITE_SOLVER_MODE=browser

# Required only for API mode when the backend is not served from the same origin.
VITE_API_BASE=http://localhost:8000
